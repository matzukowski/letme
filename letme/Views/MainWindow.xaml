<Window x:Class="letme.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        xmlns:classes="clr-namespace:letme.Classes"
        Title="{Binding Title}"
        MinWidth="1000" MinHeight="700" Width="1000" Height="700" Background="Black" ResizeMode="CanMinimize" WindowStyle="SingleBorderWindow">

    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Closing">
            <prism:InvokeCommandAction Command="{Binding ClosingCommand}"/>
        </b:EventTrigger>
    </b:Interaction.Triggers>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="{Binding ASCII}" FontSize="12" Background="Black" Foreground="White" FontFamily="Consolas" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <ContentControl Grid.Row="1" prism:RegionManager.RegionName="{x:Static classes:Names.contentRegion}"/>
    </Grid>
</Window>
