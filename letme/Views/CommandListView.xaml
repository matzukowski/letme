<UserControl x:Class="letme.Views.CommandListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:b="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:classes="clr-namespace:letme.Classes"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="b" 
             b:DesignHeight="450" b:DesignWidth="800">
    
    <UserControl.Resources>
        <classes:CommandsToPhrasesStringConverter x:Key="CommandsToPhrasesStringConverter"/>
    </UserControl.Resources>

    <Grid>
        <TextBox Margin="0" Padding="5" Background="Black" Foreground="White" BorderBrush="Transparent" FontFamily="Consolas" IsReadOnly="True" TextWrapping="Wrap"
                 Text="{Binding SpeechRecognition.Commands, Converter={StaticResource CommandsToPhrasesStringConverter}}"/>
    </Grid>
</UserControl>
